@model IEnumerable<Game>
@using GameStore.Domain.Entities
@{
    ViewBag.Title = "Список ігор";
}
@foreach (var p in @Model)
{
    <div>
        <h3>@p.Name</h3>
        <p>@p.Description</p>
        <h4>@p.Price.ToString("# грн")</h4>
    </div>
}


